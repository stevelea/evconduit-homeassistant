{
  "config": {
    "step": {
      "user": {
        "title": "Connect to EVConduit",
        "description": "Enter your API key, select environment and vehicle.",
        "data": {
          "api_key": "API Key",
          "environment": "Environment",
          "vehicle_id": "Vehicle ID",
          "abrp_token": "ABRP Token (optional)"
        }
      },
      "reconfigure": {
        "data": {
          "api_key": "API Key",
          "environment": "Environment",
          "vehicle_id": "Vehicle ID",
          "abrp_token": "ABRP Token (optional)"
        }
      }
    },
    "options": {
      "prod": "Production",
      "sandbox": "Sandbox"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "EVConduit Options",
        "description": "Configure integration settings.",
        "data": {
          "update_interval": "Update interval (minutes)",
          "odometer_entity": "Odometer sensor (auto-update after charge)"
        }
      }
    }
  }
}
